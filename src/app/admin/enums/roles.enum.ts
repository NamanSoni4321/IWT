export enum roles {
    ADMIN = 1,
    USER = 2,
}

export enum otp {
    OTP = '1423'
}

export enum PlanPurchase {
    FREE = 1,
    MONTHLY = 2,
    HALFYEARLY = 4,
    ANNUAL = 5
}

export enum PageLimit {
    Page = 1,
    Limit = 10,
}

export enum Location{
    latitude = 22.7533,
    longitude = 75.8937,
    angle = 90
}